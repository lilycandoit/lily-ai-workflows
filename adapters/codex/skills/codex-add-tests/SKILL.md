---
name: codex-add-tests
description: Add missing automated tests for existing or completed work. Use after implementation, after codex-test-plan, or when verification finds test gaps.
---

# Add tests

Follow the shared Lily workflow:

`~/.codex/custom-workflows/add-tests.md`

Use the current repository's `.planning/` folder as the project source of truth when it exists.

Do not push or commit unless the user explicitly asks or runs the git checkpoint workflow.
